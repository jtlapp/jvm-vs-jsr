if [ "$1" == "common" ]; then
    image="client"
else
    image=$1
fi

# kind load docker-image $DOCKER_IMAGE_PREFIX/$image:latest
docker push $DOCKER_IMAGE_PREFIX/$image
helm install $1 charts/$1/ -f charts/values.yaml -f charts/values-secret.yaml
